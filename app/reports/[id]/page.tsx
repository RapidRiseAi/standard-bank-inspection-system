import { ReportView } from '@/components/reports/ReportView';import { requireUser } from '@/lib/auth';import { db } from '@/lib/db';import { notFound } from 'next/navigation';
export default async function Report({params}:{params:Promise<{id:string}>}){await requireUser();const {id}=await params;const ins=await db.inspection.findUnique({where:{id},include:{inspector:true,responses:{include:{images:true}},template:{include:{sections:{orderBy:{sortOrder:'asc'},include:{items:{orderBy:{sortOrder:'asc'}}}}}}}});if(!ins)notFound();return <main className="min-h-screen bg-white"><ReportView inspection={ins}/></main>}
