import { currentUser } from '@/lib/auth';import { redirect } from 'next/navigation';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser()) redirect('/dashboard');const sp=await searchParams;return <main className="flex min-h-screen items-center justify-center bg-gradient-to-br from-slate-50 to-blue-50 p-4"><form action="/api/auth/login" method="post" className="w-full max-w-md rounded-3xl bg-white p-6 shadow-soft"><div className="mb-6"><p className="text-sm font-bold uppercase tracking-widest text-safe">BuildInspect Demo</p><h1 className="mt-2 text-3xl font-black text-navy">Secure inspection login</h1><p className="mt-2 text-slate-500">Mobile-first access for field inspection teams.</p></div>{sp.error&&<p className="mb-4 rounded-xl bg-red-50 p-3 text-sm text-red-700">{sp.error}</p>}<label className="text-sm font-semibold">Email</label><input name="email" type="email" className="mb-3 mt-1 w-full rounded-xl border p-3" defaultValue="admin@buildinspect.demo"/><label className="text-sm font-semibold">Password</label><input name="password" type="password" className="mb-5 mt-1 w-full rounded-xl border p-3" defaultValue="password123"/><button className="touch w-full rounded-xl bg-navy px-4 py-3 font-bold text-white">Sign in</button><div className="mt-5 rounded-2xl bg-slate-50 p-4 text-xs text-slate-500"><p className="font-bold text-slate-700">Demo credentials</p><p>Admin: admin@buildinspect.demo / password123</p><p>Inspector: inspector@buildinspect.demo / password123</p></div></form></main>}
