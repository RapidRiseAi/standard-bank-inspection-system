import { AppShell } from '@/components/layout/AppShell';import { requireAdmin } from '@/lib/auth';import { createTemplate } from '@/app/actions';
export default async function NewTemplate(){const user=await requireAdmin();return <AppShell user={user}><h1 className="mb-4 text-3xl font-black text-navy">Create template</h1><form action={createTemplate} className="grid gap-4 rounded-2xl bg-white p-5 shadow-soft"><label className="font-semibold">Template name<input name="name" className="mt-1 w-full rounded-xl border p-3" required/></label><label className="font-semibold">Description<textarea name="description" className="mt-1 w-full rounded-xl border p-3" required/></label><label className="font-semibold">Building type<select name="buildingType" className="mt-1 w-full rounded-xl border p-3">{['Residential','Commercial','Industrial','Mixed-use','Other'].map(x=><option key={x}>{x}</option>)}</select></label><label className="font-semibold">Default inspection category<select name="category" className="mt-1 w-full rounded-xl border p-3">{['Pre-purchase','Insurance','Safety','Maintenance','Compliance','Other'].map(x=><option key={x}>{x}</option>)}</select></label><button className="touch rounded-xl bg-navy font-bold text-white">Save template and open builder</button></form></AppShell>}
