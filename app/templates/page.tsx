import Link from 'next/link';import { AppShell } from '@/components/layout/AppShell';import { TemplateCard } from '@/components/templates/TemplateCard';import { requireUser } from '@/lib/auth';import { db } from '@/lib/db';
export default async function Templates(){const user=await requireUser();const templates=await db.template.findMany({orderBy:{updatedAt:'desc'},include:{sections:{include:{items:true}}}});return <AppShell user={user}><div className="mb-5 flex items-end justify-between gap-3"><div><h1 className="text-3xl font-black text-navy">Checklist templates</h1><p className="text-slate-500">Create reusable inspection playbooks for field teams.</p></div>{user.role==='ADMIN'&&<Link className="rounded-xl bg-navy px-4 py-3 font-bold text-white" href="/templates/new">Create Template</Link>}</div><div className="grid gap-3 md:grid-cols-2">{templates.map(t=><TemplateCard key={t.id} template={t}/>)}</div></AppShell>}
