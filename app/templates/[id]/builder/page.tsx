import { AppShell } from '@/components/layout/AppShell';import { TemplateBuilder } from '@/components/templates/TemplateBuilder';import { requireAdmin } from '@/lib/auth';import { db } from '@/lib/db';import { notFound } from 'next/navigation';
export default async function Builder({params}:{params:Promise<{id:string}>}){const user=await requireAdmin();const {id}=await params;const template=await db.template.findUnique({where:{id},include:{sections:{orderBy:{sortOrder:'asc'},include:{items:{orderBy:{sortOrder:'asc'}}}}}});if(!template)notFound();return <AppShell user={user}><h1 className="mb-4 text-3xl font-black text-navy">Template builder</h1><TemplateBuilder initial={template}/></AppShell>}
