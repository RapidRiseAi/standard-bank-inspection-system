import Link from 'next/link';
export function TemplateCard({template}:{template:any}){const sections=template.sections?.length??0;const items=template.sections?.reduce((a:any,s:any)=>a+s.items.length,0)??0;return <article className="rounded-2xl bg-white p-4 shadow-soft"><div className="flex justify-between gap-3"><div><h3 className="text-lg font-black text-navy">{template.name}</h3><p className="mt-1 text-sm text-slate-500">{template.description}</p></div><span className={`h-fit rounded-full px-2 py-1 text-xs font-bold ${template.isActive?'bg-green-100 text-green-800':'bg-slate-100 text-slate-500'}`}>{template.isActive?'Active':'Inactive'}</span></div><p className="mt-3 text-sm font-semibold text-slate-600">{sections} sections · {items} items · {template.buildingType}</p><div className="mt-4 flex flex-wrap gap-2"><Link className="rounded-xl bg-navy px-3 py-2 text-sm font-bold text-white" href={`/templates/${template.id}/builder`}>Edit</Link><form action={`/api/templates/${template.id}/duplicate`} method="post"><button className="rounded-xl border px-3 py-2 text-sm font-bold">Duplicate</button></form><Link className="rounded-xl border px-3 py-2 text-sm font-bold" href={`/templates/${template.id}/builder#preview`}>Preview</Link></div></article>}
