'use client';
const types=['CHECKBOX','TEXT','SEVERITY','PHOTO','NUMBER','DROPDOWN','MULTI_SELECT','DATE','YES_NO_NA'];
export function ChecklistItemEditor({item,onChange,onDelete}:{item:any;onChange:(i:any)=>void;onDelete:()=>void}){const set=(k:string,v:any)=>onChange({...item,[k]:v});return <div className="rounded-2xl border bg-slate-50 p-3"><div className="flex justify-between gap-2"><input className="w-full rounded-xl border p-2 font-bold" value={item.title} onChange={e=>set('title',e.target.value)} placeholder="Item title"/><button type="button" onClick={onDelete} className="text-sm font-bold text-critical">Delete</button></div><textarea className="mt-2 w-full rounded-xl border p-2 text-sm" value={item.instruction} onChange={e=>set('instruction',e.target.value)} placeholder="Inspector instruction/help text"/><div className="mt-2 grid grid-cols-2 gap-2 md:grid-cols-4"><select className="rounded-xl border p-2" value={item.responseType} onChange={e=>set('responseType',e.target.value)}>{types.map(t=><option key={t}>{t}</option>)}</select><label className="rounded-xl border bg-white p-2 text-sm"><input type="checkbox" checked={item.isRequired} onChange={e=>set('isRequired',e.target.checked)}/> Required</label><label className="rounded-xl border bg-white p-2 text-sm"><input type="checkbox" checked={item.requiresPhoto} onChange={e=>set('requiresPhoto',e.target.checked)}/> Requires photo</label><label className="rounded-xl border bg-white p-2 text-sm"><input type="checkbox" checked={item.requiresSeverity} onChange={e=>set('requiresSeverity',e.target.checked)}/> Requires severity</label><label className="rounded-xl border bg-white p-2 text-sm"><input type="checkbox" checked={item.allowPhoto} onChange={e=>set('allowPhoto',e.target.checked)}/> Allow photo</label><label className="rounded-xl border bg-white p-2 text-sm"><input type="checkbox" checked={item.isActive} onChange={e=>set('isActive',e.target.checked)}/> Active</label><input type="number" className="rounded-xl border p-2" value={item.sortOrder} onChange={e=>set('sortOrder',Number(e.target.value))}/></div>{['DROPDOWN','MULTI_SELECT'].includes(item.responseType)&&<input className="mt-2 w-full rounded-xl border p-2" value={(item.options??[]).join(', ')} onChange={e=>set('options',e.target.value.split(',').map(x=>x.trim()).filter(Boolean))} placeholder="Options separated by commas"/>}<p className="mt-2 rounded-xl bg-blue-50 p-2 text-xs font-semibold text-bank">Optional description/comment is always shown to inspectors.</p></div>}
