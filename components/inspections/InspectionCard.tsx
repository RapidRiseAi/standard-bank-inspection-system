import Link from 'next/link';import { ProgressBar } from '@/components/ui/ProgressBar';import { SeverityBadge } from '@/components/ui/SeverityBadge';
export function InspectionCard({inspection}:{inspection:any}){const progress=inspection.progress??0;return <article className="rounded-2xl bg-white p-4 shadow-soft"><div className="flex items-start justify-between gap-3"><div><p className="font-black text-navy">{inspection.clientName}</p><h3 className="text-lg font-bold">{inspection.propertyName}</h3><p className="text-sm text-slate-500">{inspection.propertyAddress}</p></div><SeverityBadge severity={inspection.status}/></div><div className="mt-4"><div className="mb-1 flex justify-between text-xs font-bold text-slate-500"><span>Progress</span><span>{progress}%</span></div><ProgressBar value={progress}/></div><div className="mt-4 flex flex-wrap gap-2 text-sm"><Link className="rounded-xl bg-navy px-3 py-2 font-bold text-white" href={`/inspections/${inspection.id}`}>{inspection.status==='COMPLETED'?'View':'Continue'}</Link><Link className="rounded-xl border px-3 py-2 font-bold" href={`/inspections/${inspection.id}/summary`}>Summary</Link><Link className="rounded-xl border px-3 py-2 font-bold" href={`/reports/${inspection.id}`}>Report</Link></div></article>}
