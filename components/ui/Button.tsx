import { clsx } from 'clsx';
import type { ButtonHTMLAttributes, AnchorHTMLAttributes } from 'react';
export function Button({className,variant='primary',...p}:ButtonHTMLAttributes<HTMLButtonElement>&{variant?:'primary'|'secondary'|'danger'|'ghost'}){return <button className={clsx('touch rounded-xl px-4 py-3 font-semibold transition disabled:opacity-50',variant==='primary'&&'bg-navy text-white hover:bg-bank',variant==='secondary'&&'border border-slate-200 bg-white text-slate-800',variant==='danger'&&'bg-critical text-white',variant==='ghost'&&'text-bank hover:bg-slate-100',className)} {...p}/>}
export function LinkButton({className,variant='primary',...p}:AnchorHTMLAttributes<HTMLAnchorElement>&{variant?:'primary'|'secondary'|'ghost'}){return <a className={clsx('touch inline-flex items-center justify-center rounded-xl px-4 py-3 font-semibold',variant==='primary'&&'bg-navy text-white',variant==='secondary'&&'border border-slate-200 bg-white text-slate-800',variant==='ghost'&&'text-bank',className)} {...p}/>}
