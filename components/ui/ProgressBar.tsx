export function ProgressBar({value}:{value:number}){return <div className="h-3 rounded-full bg-slate-200"><div className="h-3 rounded-full bg-safe transition-all" style={{width:`${Math.min(100,Math.max(0,value))}%`}} /></div>}
