import Link from 'next/link';import { ClipboardList, Home, Layers, PlusCircle } from 'lucide-react';
export function MobileBottomNav(){const items=[['/dashboard',Home,'Home'],['/inspections',ClipboardList,'Inspections'],['/inspections/new',PlusCircle,'Start'],['/templates',Layers,'Templates']] as const;return <nav className="fixed inset-x-0 bottom-0 z-40 border-t border-slate-200 bg-white/95 backdrop-blur md:hidden"><div className="grid grid-cols-4">{items.map(([href,Icon,label])=><Link key={href} href={href} className="flex flex-col items-center gap-1 px-2 py-2 text-xs font-semibold text-slate-600"><Icon size={20}/>{label}</Link>)}</div></nav>}
